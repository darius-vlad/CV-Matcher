# imports

def add_cvs(cvs): # actual docx/pdf file
    # retrieve all JOB vector embeddings
    # for cv in cvs:
    #   cv_embedding, sim_measures = process_cv(cv, job_vector_embeddings)
    # save cv_vector_embedding in db
    # insert new sim_measure 'row'
    pass